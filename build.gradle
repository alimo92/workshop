repositories {
    mavenCentral()
}

subprojects {
    apply plugin: "java"

    test {
        useJUnitPlatform()
        testLogging {
            events "passed", "skipped", "failed"
        }
    }
}
